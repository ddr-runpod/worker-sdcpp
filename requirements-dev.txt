# Test-only dependencies; not required to run the worker in production.
# Install before running `pytest`:
#   uv pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
requests-mock>=1.12
